import * as d3 from "d3";

function SchemaVisual({ schema }) {

    return (
        <div className="container">
            <h1>schema visual here</h1>
        </div>
    )
}

export default SchemaVisual